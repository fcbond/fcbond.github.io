# fcbond.github.io
Francis Bond's humble homepage


Made with `frozen-flask` from files in `web`

run `bash run.sh` to test it locally
run `bash freeze.sh` to freeze the files in the `docs` directory
